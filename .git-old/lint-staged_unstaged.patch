diff --git a/src/test/modules/Auth/Application/RevokeSession/RevokeSession.unit.spec.ts b/src/test/modules/Auth/Application/RevokeSession/RevokeSession.unit.spec.ts
index 21a39e3..2f86c95 100644
--- a/src/test/modules/Auth/Application/RevokeSession/RevokeSession.unit.spec.ts
+++ b/src/test/modules/Auth/Application/RevokeSession/RevokeSession.unit.spec.ts
@@ -206 +206 @@ describe('RevokeSession', () => {
-      expect(mockedLogger.error).toHaveBeenCalledWith('Inconsistent state', undefined, {
+      expect(mockedLogger.warn).toHaveBeenCalledWith('Session revocation failed', {
@@ -209 +209 @@ describe('RevokeSession', () => {
-        reason: 'Attempted to revoke a non-existent session',
+        reason: 'Session not found',
@@ -228 +228 @@ describe('RevokeSession', () => {
-      expect(mockedLogger.error).toHaveBeenCalledWith('Inconsistent state', undefined, {
+      expect(mockedLogger.warn).toHaveBeenCalledWith('Session revocation rejected', {
@@ -232 +232 @@ describe('RevokeSession', () => {
-        reason: 'Session owner mismatch during revocation',
+        reason: 'Session owner mismatch',
